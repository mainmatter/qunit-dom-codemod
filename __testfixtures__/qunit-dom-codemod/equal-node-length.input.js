assert.equal(node.length, 1);

assert.equal(node.length, 1, 'custom message');

assert.equal(node.length, 0);

assert.equal(node.length, 0, 'custom message');

assert.equal(node, 'bar');

assert.equal(true);
